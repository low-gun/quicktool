// frontend/components/DocumentOptions.js
export default function DocumentOptions({ docOption, setDocOption }) {
  // 예: OCR 여부, 특정 페이지 범위, 등등
  // 여기서는 예시가 없으니 빈 UI
  return (
    <div
      style={{ background: "#fafafa", padding: "10px", borderRadius: "8px" }}
    >
      <h3>문서 변환 옵션</h3>
      <p>예) OCR 적용, 페이지 범위, etc.</p>
    </div>
  );
}
